# jpzip web

`jpzip.nadai.dev` のランディングページ。

Cloudflare Pages プロジェクト `jpzip` の初回ブートストラップ用の最小サイト。本番のデータ (`/all.json`, `/p/*.json`, `/meta.json` 等) は [`jpzip/data`](https://github.com/jpzip/data) の GitHub Actions が `wrangler pages deploy` で上書きデプロイする。

## ライセンス

MIT
